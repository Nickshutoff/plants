let spoiler = document.querySelectorAll(".site");
let activeIndex = null;
console.log(spoiler)

spoiler.forEach => (){
   el.EventTarget.addEventListener("click", () => {
    activeIn
    })
}